#include <stdio.h>
int main()
{
    int nums[] = {2, 3, 1, 1, 4};
    int n = sizeof(nums) / sizeof(nums[0]);

    int index = nums[0];

    for (int i = 0; i < n; i++)
    {
        
    }
}